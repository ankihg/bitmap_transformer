'use strict';
/*const expect = require('chai').expect;
const fs = require('fs');
const fileManager = require(__dirname + '/../lib/fileManager');

describe('testing for file manager', function() {

  var bitmapBufTest;
  var rPath = '/media/palette-bitmap.bmp';

  before(function(done) {
    fs.readFile(__dirname + '/../' + rPath, function(err, data) {
      if (err) throw err;
      bitmapBufTest = data;
      done();
    });
  });

  it('should return data from file', function(done) {
    var bitmapBufGetter = fileManager.read(rPath);
    expect(bitmapBufGetter).eql(bitmapBufTest);
    done();
  });
});*/
